#!/usr/bin/env python3
"""
修改 dataset.json 中所有 MEDIUM 样本的 student_answer，
使其只覆盖约一半得分点，目标得分 5-7 分，以和 HIGH（8-10分）拉开区分度。
同时调整 human_score 和 expected_score_range。
"""
import json
import os

SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))
INPUT_FILE = os.path.join(SCRIPT_DIR, "dataset.json")

with open(INPUT_FILE, "r", encoding="utf-8") as f:
    data = json.load(f)

# 为每道题定义新的 MEDIUM 答案（只覆盖部分得分点）
# question_id -> new_answer 映射
# 设计原则：每个答案遗漏 40-50% 的得分点，目标得分 5-7 分

medium_rewrites = {
    # Q1: TCP三次握手 (总分10, 8个得分点)
    # 覆盖: point1(建立连接,2分), point4(第一次握手,1分), point5(第二次握手,1分), point6(第三次握手,1分) = 5分
    # 遗漏: point2(同步序列号,2分), point3(防失效请求,2分), point7(双方能力,0.5分), point8(协商序列号,0.5分) = 5分
    1: {
        "answer": "三次握手是为了建立TCP连接。具体过程是：第一步客户端发送SYN报文，第二步服务器返回SYN-ACK报文，第三步客户端再发ACK报文确认。通过这三步就完成了连接的建立，之后就可以传输数据了。",
        "human_scores": [6.0, 5.5, 6.0],
        "expected_range": [4, 7]
    },

    # Q2: 进程和线程 (总分10, 5个得分点各2分)
    # 覆盖: point1(进程定义,2分), point3(资源共享通信,2分) = 4分; 部分覆盖 point2(线程定义,约1分)
    # 遗漏: point4(开销区别,2分), point5(崩溃影响,2分)
    2: {
        "answer": "进程是程序运行的一个实例，有自己独立的内存空间和系统资源。线程是进程里面更小的执行单元。它们之间的区别主要是进程之间内存不共享，通信比较麻烦，而线程共享进程的内存空间，所以线程之间的通信比较方便，但需要注意同步问题。",
        "human_scores": [6.0, 5.0, 5.5],
        "expected_range": [4, 7]
    },

    # Q3: ACID特性 (总分10, 4个得分点各2.5分)
    # 覆盖: point1(原子性,2.5分), point4(持久性,2.5分) = 5分
    # 遗漏: point2(一致性), point3(隔离性) —— 只提了名字没解释
    3: {
        "answer": "ACID是数据库事务的四个特性。原子性是指事务要么全部执行成功，要么全部失败回滚，不能只做一半。持久性是指事务一旦提交，数据就会永久保存下来，即使系统出了故障也不会丢。另外还有一致性和隔离性，但具体定义记不太清楚了。",
        "human_scores": [6.0, 5.5, 5.5],
        "expected_range": [4, 7]
    },

    # Q4: BST (总分10, 4个得分点: 3+2+2+3)
    # 覆盖: point1(定义,3分), point2(查找复杂度,部分约1分) = 4分
    # 遗漏: point3(插入), point4(删除三种情况)
    4: {
        "answer": "二叉搜索树是一种特殊的二叉树，要求左子树所有节点的值小于根节点，右子树所有节点的值大于根节点。查找操作的时间复杂度大概是O(log n)。插入和删除也差不多是这个复杂度，具体过程不太记得了。",
        "human_scores": [5.5, 5.0, 5.5],
        "expected_range": [4, 6]
    },

    # Q5: GET和POST (总分10, 5个得分点各2分)
    # 覆盖: point1(参数位置,2分), point3(长度限制,2分) = 4分; 部分point2(安全性,约1分)
    # 遗漏: point4(幂等性), point5(缓存)
    5: {
        "answer": "GET和POST的区别主要有：GET请求的参数会放在URL后面，POST的参数放在请求体里面。GET因为参数在网址里能看到，所以不太安全。另外GET传输的数据量有限制，URL不能太长，而POST可以传比较大的数据。一般获取数据用GET，提交数据用POST。",
        "human_scores": [6.0, 5.5, 5.0],
        "expected_range": [4, 7]
    },

    # Q6: 死锁 (总分10, 5个得分点: 3+2+2+2+1)
    # 覆盖: point1(定义,3分), point2(互斥,2分) = 5分
    # 遗漏: point3(请求保持), point4(不可剥夺), point5(循环等待)
    6: {
        "answer": "死锁是指多个进程因为竞争资源而陷入互相等待的状态，谁都没法继续执行下去。产生死锁需要满足一些条件，其中一个是互斥条件，就是资源在同一时间只能被一个进程使用，不能共享。其他几个条件好像跟资源的分配方式有关，但具体名称和内容记不清了。",
        "human_scores": [5.5, 5.0, 5.5],
        "expected_range": [4, 6]
    },

    # Q7: 数据库范化/1NF (总分10, 4个得分点: 4+3+2+1)
    # 覆盖: point2(1NF核心要求,3分), point4(范化目的,1分) = 4分; 部分point1(范化定义,约1.5分)
    # 遗漏: point3(1NF具体要求的深入解释)
    7: {
        "answer": "数据库规范化就是通过一些规则来整理表的结构。第一范式要求每个字段里的值不能再拆分了，必须是原子的，比如不能把多个电话号码存在同一个字段里。这样做是为了减少数据的冗余。",
        "human_scores": [6.5, 6.0, 6.0],
        "expected_range": [5, 7]
    },

    # Q8: 快速排序 (总分10, 5个得分点: 2+3+2+2+1)
    # 覆盖: point1(分治法,2分), point4(平均O(nlogn),2分) = 4分; 部分point2(分区过程,约1.5分)
    # 遗漏: point3(递归排序子数组), point5(最坏O(n²))
    8: {
        "answer": "快速排序是一种基于分治思想的排序算法。它的做法是从数组里选一个基准元素，然后把比基准小的放一边，比基准大的放另一边。平均时间复杂度是O(n log n)，效率比较高，是常用的排序算法之一。",
        "human_scores": [6.0, 5.5, 6.0],
        "expected_range": [4, 7]
    },

    # Q9: IP和MAC地址 (总分10, 4个得分点: 3+2+3+2)
    # 覆盖: point2(可变性,2分), point3(各自作用,部分约1.5分) = 3.5分; 部分point1(层次区别,约1分)
    # 遗漏: point4(格式区别)
    9: {
        "answer": "IP地址和MAC地址都是用来标识网络设备的。IP地址可以变化，比如连不同的WiFi会分配不同的IP。MAC地址是出厂就固定的，一般不会变。IP地址主要用来在不同网络之间寻址通信，MAC地址主要在局域网里识别设备。",
        "human_scores": [6.0, 5.5, 6.5],
        "expected_range": [5, 7]
    },

    # Q10: 虚拟内存 (总分10, 5个得分点: 3+2+2+2+1)
    # 覆盖: point2(扩大空间,2分), point1(定义,部分约1.5分) = 3.5分
    # 遗漏: point3(内存隔离), point4(简化编程), point5(关键技术)
    10: {
        "answer": "虚拟内存是一种内存管理技术，可以把硬盘的一部分空间当做内存来使用。这样即使物理内存不够，也可以运行比较大的程序，相当于扩展了可用的内存空间。需要的数据会从硬盘调入内存，不用的会暂时放回硬盘。",
        "human_scores": [5.5, 5.0, 5.0],
        "expected_range": [4, 6]
    },

    # Q11: HTTP vs HTTPS (总分10)
    # 原始 MEDIUM 已有，从 grep 结果可知 行号 3291
    # 覆盖: 安全加密(2分), 端口号(2分) = 4分; 部分提到证书(约1分)
    # 遗漏: 性能影响, 连接过程(握手)
    11: {
        "answer": "HTTP和HTTPS的区别主要是安全性方面。HTTPS比HTTP更安全，因为HTTPS对传输的数据进行了加密，HTTP是明文传输的。另外它们的端口号不一样，HTTP用的是80端口，HTTPS用的是443端口。HTTPS好像需要申请某种证书才能用。",
        "human_scores": [6.0, 5.5, 5.5],
        "expected_range": [4, 7]
    },

    # Q12: 死锁(重复题) (总分10, 行号 3515)
    # 覆盖: 定义(3分), 互斥(2分), 请求保持(2分) = 7分
    # 遗漏: 不可剥夺, 循环等待
    12: {
        "answer": "死锁是多个进程互相等待对方释放资源，导致都无法继续执行的状态。产生死锁的条件有：互斥条件，就是资源只能被一个进程独占使用；还有请求与保持条件，进程已经持有资源了还要申请新的资源。其他条件不太记得了。",
        "human_scores": [6.5, 7.0, 6.5],
        "expected_range": [5, 7]
    },

    # Q13: 数据库三范式 (总分10, 行号 3734)
    # 覆盖: 1NF(描述), 2NF(描述), 但遗漏 3NF 的正确描述和范化意义
    13: {
        "answer": "数据库有三个范式。第一范式要求每个字段不能再分，必须是原子值。第二范式要求非主属性完全依赖于主键，不能只依赖主键的一部分。第三范式的具体要求记不太清了，好像跟依赖关系有关。",
        "human_scores": [6.0, 5.5, 6.0],
        "expected_range": [4, 7]
    },

    # Q14: 顺序存储和链式存储 (总分10, 行号 4031)
    # 覆盖: 顺序存储的优点(随机访问快), 链式存储的优点(插入删除方便)
    # 遗漏: 顺序存储的缺点(需连续空间/插入删除慢), 链式存储的缺点(不能随机访问/额外空间), 比较总结
    14: {
        "answer": "顺序存储结构就是用数组来存数据，优点是可以通过下标直接访问元素，速度很快。链式存储结构是用链表来存数据，每个节点通过指针连起来，插入和删除元素比较方便，不需要移动其他元素。两种方式各有各的好处。",
        "human_scores": [5.5, 5.0, 5.5],
        "expected_range": [4, 6]
    },

    # Q15: DNS解析 (总分10, 行号 4318)
    # 覆盖: 基本概念(客户端发起请求), 本地DNS缓存, 递归查询过程(部分)
    # 遗漏: 根域名服务器角色, 迭代查询过程细节, 权威DNS, 缓存机制
    15: {
        "answer": "DNS解析就是把域名转换成IP地址的过程。当用户输入网址后，电脑会向本地DNS服务器发起查询。如果本地DNS服务器有缓存就直接返回结果。如果没有缓存的话，它会去网上查找，最终拿到对应的IP地址返回给用户。",
        "human_scores": [5.0, 5.5, 5.0],
        "expected_range": [4, 6]
    },

    # Q16: 虚拟内存(重复题) (总分10, 行号 4591)
    # 覆盖: 定义(部分), 扩大空间作用
    # 遗漏: 内存保护/隔离, 共享, 请求分页机制
    16: {
        "answer": "虚拟内存是一种技术，能让程序觉得自己有很大的连续内存可以使用。实际上物理内存可能不够，但通过虚拟内存技术可以把硬盘空间也利用起来，让电脑能够运行更大的程序。不过用硬盘当内存的话速度会慢一些。",
        "human_scores": [5.0, 5.5, 5.0],
        "expected_range": [4, 6]
    },

    # Q17: TCP vs UDP (总分10, 行号 4816)
    # 覆盖: 连接性区别, 可靠性区别
    # 遗漏: 开销/头部大小, 应用场景具体例子
    17: {
        "answer": "TCP和UDP的区别主要有两点：TCP是面向连接的，需要先建立连接才能通信；UDP不需要建立连接，直接发数据就行。另外TCP能保证数据可靠到达，有重传机制；UDP不保证可靠性，数据可能会丢失。",
        "human_scores": [5.0, 5.5, 5.5],
        "expected_range": [4, 6]
    },

    # Q18: 进程和线程(重复题) (总分10, 行号 5036)
    # 覆盖: 进程定义(部分), 线程定义(部分), 通信区别
    # 遗漏: 开销区别, 崩溃影响/健壮性
    18: {
        "answer": "进程就是程序运行起来后的一个实例，线程是进程里面的执行单元。两者的区别是进程有独立的内存空间，而线程共享所在进程的内存。所以线程之间通信比进程之间方便。",
        "human_scores": [5.0, 5.5, 5.0],
        "expected_range": [4, 6]
    },

    # Q19: ACID(重复题) (总分10, 行号 5247)
    # 覆盖: 原子性(描述正确), 持久性(描述正确)
    # 遗漏: 一致性(只提名字), 隔离性(只提名字)
    19: {
        "answer": "ACID是数据库事务的四个特性。原子性就是事务要么全部做完，要么全部不做。持久性是事务提交后数据会永久保存。还有一致性和隔离性，大概跟数据正确和事务独立有关吧。",
        "human_scores": [6.0, 5.0, 5.5],
        "expected_range": [4, 7]
    },

    # Q20: 数组和链表(重复题) (总分10, 行号 5458)
    # 覆盖: 存储方式区别, 链表插入删除优势
    # 遗漏: 数组随机访问O(1), 链表顺序访问O(n), 空间开销区别
    20: {
        "answer": "数组是在内存中连续存储数据的，链表是通过指针把各个节点串起来的。链表在插入和删除数据时比较方便，不需要移动其他元素。数组的话插入删除就麻烦一些。",
        "human_scores": [5.0, 5.5, 5.5],
        "expected_range": [4, 6]
    },

    # Q21: OSI七层 (总分10, 行号 5744)
    # 覆盖: 列出七层名称, 物理层功能, 网络层功能
    # 遗漏: 数据链路层功能, 传输层功能, 会话层/表示层/应用层功能
    21: {
        "answer": "OSI模型把网络通信分为七层，从下到上分别是物理层、数据链路层、网络层、传输层、会话层、表示层和应用层。物理层负责传输原始的比特流，网络层负责路由和转发数据包。其他层的具体功能记得不太清楚了。",
        "human_scores": [5.5, 5.0, 5.5],
        "expected_range": [4, 6]
    },

    # Q22: 死锁(第三次重复) (总分10, 行号 6015)
    # 覆盖: 定义, 互斥条件, 循环等待(部分)
    # 遗漏: 请求与保持, 不可剥夺
    22: {
        "answer": "死锁是多个进程因为争抢资源而卡住的情况，大家都在等对方先放手。条件之一是互斥，资源同一时间只能给一个进程用。另外进程之间好像要形成一个循环等待的关系才会死锁。其他条件想不起来了。",
        "human_scores": [5.5, 6.0, 5.5],
        "expected_range": [4, 7]
    },

    # Q23: 三范式(重复) (总分10, 行号 6219)
    # 覆盖: 1NF(属性不可再分), 3NF(消除传递依赖)
    # 遗漏: 2NF(消除部分依赖的解释), 范化目的和意义
    23: {
        "answer": "第一范式要求字段是不可再分的原子值。第三范式是消除传递依赖，非主属性不能依赖另一个非主属性。第二范式的具体要求不太记得了，好像跟主键有关。",
        "human_scores": [5.5, 5.0, 5.5],
        "expected_range": [4, 6]
    },

    # Q24: 二叉树遍历(重复) (总分10, 行号 6400)
    # 覆盖: 先序遍历定义(根左右), 中序遍历定义(左根右)
    # 遗漏: 后序遍历(描述错误或遗漏), 递归特性, 应用场景
    24: {
        "answer": "二叉树遍历有三种方式。先序遍历是先访问根节点，然后左子树，再右子树。中序遍历是先左子树，再根节点，最后右子树。后序遍历的顺序不太记得了，好像是先处理子树再处理根节点。",
        "human_scores": [5.5, 6.0, 5.5],
        "expected_range": [4, 7]
    },

    # Q25: HTTP无状态/Cookie/Session (总分10, 行号 6574)
    # 覆盖: HTTP无状态的含义, Cookie的基本概念
    # 遗漏: Session的详细机制(Session ID如何传递), Cookie和Session的关联
    25: {
        "answer": "HTTP协议是无状态的，每次请求之间是独立的，服务器不会记住前一次请求的信息。Cookie是服务器发给浏览器的一小段数据，浏览器保存下来，下次访问同一个网站时会自动带上。Session好像也是用来保持状态的，存在服务器那边，但具体怎么工作不太清楚。",
        "human_scores": [6.0, 5.5, 5.5],
        "expected_range": [4, 7]
    },

    # Q26: 虚拟内存(第三次重复) (总分10, 行号 6771)
    # 覆盖: 基本概念(用硬盘扩展内存), 保护作用(部分)
    # 遗漏: 按需分页机制, 提高多道程序并发能力, 提高利用率
    26: {
        "answer": "虚拟内存可以让电脑用硬盘空间来弥补物理内存不足的问题，这样就可以运行更大的程序。它还能防止不同程序之间互相干扰内存，有一定的保护作用。不过用虚拟内存会导致速度变慢。",
        "human_scores": [5.5, 5.0, 5.5],
        "expected_range": [4, 6]
    },
}

# 遍历数据集并修改 MEDIUM 样本
medium_count = 0
for item in data:
    if item.get("quality") == "MEDIUM":
        medium_count += 1
        qid = item["question_id"]

        # 查找对应的 rewrite
        if medium_count in medium_rewrites:
            rewrite = medium_rewrites[medium_count]
            item["student_answer"] = rewrite["answer"]
            item["human_score_1"] = rewrite["human_scores"][0]
            item["human_score_2"] = rewrite["human_scores"][1]
            item["human_score_3"] = rewrite["human_scores"][2]
            item["human_score_avg"] = round(sum(rewrite["human_scores"]) / 3, 2)
            item["expected_score_range"] = rewrite["expected_range"]

            # 清除旧的 AI 评分（需要重新评测）
            for key in ["lenient_score", "strict_score", "free_score", "keyword_score",
                        "original_lenient_score", "original_strict_score",
                        "enhanced_lenient_score", "enhanced_strict_score"]:
                if key in item:
                    del item[key]

            print(f"  ✅ MEDIUM #{medium_count} (Q{qid}): human_avg={item['human_score_avg']}, range={rewrite['expected_range']}")
        else:
            print(f"  ⚠️ MEDIUM #{medium_count} (Q{qid}): 未定义重写，保持不变")

print(f"\n共处理 {medium_count} 个 MEDIUM 样本")

# 写回文件
with open("dataset.json", "w", encoding="utf-8") as f:
    json.dump(data, f, ensure_ascii=False, indent=2)

print("✅ dataset.json 已更新")
